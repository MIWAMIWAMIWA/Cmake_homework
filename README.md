# Linux assigment homework
Compile C library static and dynamic version with C++ aplication using Cmake
## Prerequisites
```bash
sudo apt get cmake
```
## To build
```bash
cd build 
cmake ..
make
```
### There are alsp .png`s with results of execution